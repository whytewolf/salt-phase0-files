{% import 'macros.jinja' as macros %}
{% import 'map.jinja' import idents with context %}
{{ macros.salt_warning() }}

# MAPNAME       SYSTEM-USERNAME         PG-USERNAME
{% for ident in idents %}
{{ macros.ident_line(ident) }}
{% endfor %}
